maintainers:
  - github: EHfive
    email: eh5@eh5.me

update_on:
  # - source: github
  #   github: zen-kernel/zen-kernel
  #   use_max_tag: true
  #   include_regex: 'v[0-9.]+-zen[0-9]'
  #   from_pattern: 'v([0-9.]+)-(zen[0-9])'
  #   to_pattern: '\1.\2'
  - source: git
    git: https://gitlab.archlinux.org/archlinux/packaging/packages/linux-zen.git
    use_commit: true
  - source: archpkg
    archpkg: linux-zen
